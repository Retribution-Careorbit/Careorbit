Here is every service, what it does specifically in CareOrbit, why it was chosen over alternatives, the exact package, and the mock path for tests.
#ServiceWhat It Does in CareOrbitWhy This Over AlternativesPackageTest Mock Path1Azure AI Document IntelligenceOCR on prescription photos, medicine strips, lab reports — extracts structured tables, handwriting, printed textChosen over Computer Vision Read API because the prebuilt-document model understands medical document layout (lab report tables, key-value pairs like "HbA1c: 7.8%") nativelyazure-ai-documentintelligence==1.0.0services.azure_vision.AzureVisionService.extract_text2Azure OpenAI (GPT-4o)Structured medication extraction from OCR text, care gap reasoning, multi-agent response synthesis, Hindi medical term understandingGPT-4o over GPT-3.5: Hindi abbreviations (BD/OD/HS/SOS), brand-to-generic mapping (Thyronorm→Levothyroxine), multi-hop reasoning for PHIGopenai==1.35.0services.azure_openai.AzureOpenAIService.extract_structured_data3Azure AI TranslatorHindi ↔ English for patient queries and AI responsesNo alternative — only service with Indian language support at this quality level. Uses httpx directly (no async Azure Translator SDK exists)httpx==0.27.0services.azure_translator.httpx.AsyncClient4Azure AI Language (Healthcare NER)Extract medication names, dosages, diagnoses from OCR text with direct RxNorm and ICD-10 entity linkingChosen over generic NER because it returns dataSourceEntityId pointing directly to RxNorm codes — eliminates the fuzzy-match step for well-structured textazure-ai-textanalytics==5.3.0services.azure_language.TextAnalyticsClient5Azure Blob StorageStore uploaded prescription images; serve generated health summary PDFs via time-limited SAS URLsRequired — no alternative. PDFs can't live in Postgres (size + streaming)azure-storage-blob==12.20.0services.azure_blob.BlobServiceClient6Azure Communication Services (Email)Medication reminder emails, upload result notifications, drug interaction alert emailsChosen over SendGrid: single Azure billing, HIPAA-eligible service tier, begin_send() returns a poller (async-friendly)azure-communication-email==1.0.0services.azure_email.EmailClient.begin_send7Azure AI SearchRAG for drug interaction lookup (10K+ drug pairs indexed with vectors) and clinical guideline retrieval for care gap detectionChosen over Pinecone/Weaviate: hybrid keyword+vector search catches both spelling variants of drug names AND semantic similarity. Stays in Azure billingazure-search-documents==11.6.0services.azure_search.SearchClient.search8Azure PostgreSQL Flexible ServerAll relational data — users, PHIG nodes/edges, audit_log, reminders, subscriptions, usage trackingChosen over Cosmos DB: pgcrypto extension for pgp_sym_encrypt PII encryption at rest, ACID transactions for append-only audit_log, HIPAA BAA availableasyncpg==0.29.0 + sqlalchemy==2.0.30Real DB in Docker for integration tests (pytest-asyncio)9Azure Key VaultENCRYPTION_KEY (for pgcrypto), JWT_SECRET, all Azure API keysRequired for production: secret rotation without redeployment, audit trail for every key access, HIPAA requirementazure-keyvault-secrets==4.8.0services.azure_keyvault.SecretClient.get_secret10Azure Static Web AppsHosting Next.js frontendFirst-class Next.js support, built-in staging slots, global CDN, free tier appropriate for demo/competition(deployment target, no SDK)